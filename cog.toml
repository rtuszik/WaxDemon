tag_prefix = "v"
pre_bump_hooks = [
    "cargo build --release",
    "cargo set-version --workspace {{version}}",
    "cargo check --release",
    "scripts/bump-version {{version}}",
]

[changelog]
template = "remote"
remote = "github.com"
owner = "rtuszik"
repository = "WaxDemon"

[[changelog.authors]]
signature = "Robin Tuszik"
username = "rtuszik"
