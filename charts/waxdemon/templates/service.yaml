apiVersion: v1
kind: Service
metadata:
  name: {{ include "waxdemon.fullname" . }}
  labels:
    {{- include "waxdemon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waxdemon.selectorLabels" . | nindent 4 }}
